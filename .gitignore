# Local-only plans/lessons and Claude files (never commit)
plan/
.claude/
CLAUDE.md

# Secrets & runtime state (local-only; never commit)
.env
data/
deploy/local.conf

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
